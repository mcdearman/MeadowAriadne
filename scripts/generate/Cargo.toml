[package]
name = "generate"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
ariadne = { version = "=0.6.0", default-features = false }
# The versions ariadne draws with, pinned so that its output cannot move
# under the fingerprint.
yansi = "=1.0.1"
unicode-width = "=0.2.2"

[build-dependencies]
serde_json = "1"
