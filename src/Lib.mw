-- ariadne: fancy diagnostics for compilers and other tools that point into
-- text. A port of Rust's `ariadne` crate, version 0.6.0, measuring text with
-- this ecosystem's `unicodeWidth`.
--
--   use Nettle (build, withMessage, withLabel, label, labelMessage, spanIn, sources, render)
--
--   def report =
--     build Error (spanIn "a.mw" 8 11)
--       |> withMessage "unknown name"
--       |> withLabel (label (spanIn "a.mw" 8 11) |> labelMessage "not in scope")
--
--   render report (sources [("a.mw", "def x = foo\n")])
--     -- Error: unknown name
--     --    ╭─[ a.mw:1:9 ]
--     --    │
--     --  1 │ def x = foo
--     --    │         ─┬─
--     --    │          ╰─── not in scope
--     -- ───╯
--
-- (with colour escapes, unless the configuration turns colour off).

mod Color
mod Source
mod Report
mod Write
@cfg(test)
mod Cases
@cfg(test)
mod Tests

@pub use Nettle.Color
@pub use Nettle.Color.Color.*
@pub use Nettle.Source
@pub use Nettle.Report
@pub use Nettle.Report.LabelAttach.*
@pub use Nettle.Report.CharSet.*
@pub use Nettle.Report.IndexType
@pub use Nettle.Report.ReportKind.*
@pub use Nettle.Write (Characters, unicodeCharacters, asciiCharacters, render, print)
